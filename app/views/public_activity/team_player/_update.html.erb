<%-
  old_team = Team.find(a.parameters[:old_team])
  new_team = Team.find(a.parameters[:new_team])
-%>

<%= link_to old_team, old_team %> <strong>sold</strong> <%= link_to a.trackable.short_name, team_player_path(a.trackable.team, a.trackable) %> (<%= a.trackable.club.short_name %>) to <%= link_to new_team, new_team %> for &pound;<%= a.parameters[:purchase_price] %>m.