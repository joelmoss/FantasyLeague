<%-
  team = Team.find(a.parameters[:old_team])
  TeamPlayer.unscoped { a.trackable }
  player = Player.unscoped { a.trackable.player }
-%>

<%= link_to team, team %> <strong>released</strong> <%= link_to player.short_name, player %>, who is now a free agent.