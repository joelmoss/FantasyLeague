<%-
  team = Team.find(p[:new_team])
  TeamPlayer.unscoped { a.trackable }
-%>

<%= link_to team, team %> <strong>purchased</strong> <%= link_to a.trackable.short_name, team_player_path(team, a.trackable) %> (<%= a.trackable.club.short_name %>) for &pound;<%= a.trackable.purchase_price %>m.