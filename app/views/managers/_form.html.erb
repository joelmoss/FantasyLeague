<%= simple_form_for @manager do |f| %>
  <%= f.input :name, placeholder: 'Managers name...', required: true %>
  <%= f.input :email, required: true %>
  <%= f.input :mobile_number, required: true %>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
