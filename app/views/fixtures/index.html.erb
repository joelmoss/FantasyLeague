<%= content_for :title, 'Fixtures' %>
<h1 class="page-header"><%= content_for :title %> <small>with results</small></h1>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th width="160" class="text-right">Date</th>
      <th></th>
      <th></th>
      <th class="text-center">Score</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <%= render @fixtures %>
  </tbody>
</table>
